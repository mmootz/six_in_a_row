
import 'package:flutter/material.dart';

class PlayerInfo extends StatelessWidget {
  const PlayerInfo({Key? key}) : super(key: key);
  // take in player name and display in box
  // name  wins
  // highest score
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
