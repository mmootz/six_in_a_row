
import 'package:flutter/material.dart';

class getScores extends StatelessWidget {
  const getScores({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
